program: train.py
method: grid
metric:
  name: best_val_acc
  goal: maximize
parameters:
  exec_name: # change to easily identify
    values: ["resume:"]
  fold_num:
    values: [0]
  resume_checkpoint_dir:
    values: [""]
  # ... other parameters