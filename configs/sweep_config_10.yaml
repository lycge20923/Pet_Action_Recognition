program: train.py
method: grid
metric:
  name: best_val_acc
  goal: maximize
parameters:
  exec_name: # change to easily identify
    values: ["stgcn"]
  fold_num:
    values: [0]
  # ... other parameters
  epochs:
    values: [10000]
  batch_size:
    values: [128]
  add_optical_flow:
    values: [False]
  add_gate_node:
    values: [True]