program: train.py
method: grid
metric:
  name: best_val_acc
  goal: maximize
parameters:
  fold_num:
    values: [0, 1, 2, 3, 4]
  exec_name: # change to easily identify
    values: ["formal:i3dgcn stream"]
  is_i3dgcn_stream:
    values: [True]
  patient_epochs:
    values: [100]
  # other parameters...