program: train.py
method: grid
metric:
  name: best_val_acc
  goal: maximize
parameters:
  fold_num:
    values: [0]
  exec_name: # change to easily identify
    values: ["formal:flowgcn & I3D"]
  # other parameters...
