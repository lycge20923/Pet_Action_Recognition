program: train.py
method: grid
metric:
  name: best_val_acc
  goal: maximize
parameters:
  exec_name: # change to easily identify
    values: ["stgcn_I3D_middle_fusion"]
  fold_num:
    values: [0]
  # ... other parameters
  epochs:
    values: [200]
  batch_size:
    values: [8]
  use_mid_level_fusion:
    values: [True]