program: train.py
method: grid
metric:
  name: best_val_acc
  goal: maximize
parameters:
  exec_name: # change to easily identify
    values: ["experiment:this is a test"]
  fold_num:
    values: [0]
  
  # ... other parameters
  epochs:
    values: [3000]
  batch_size:
    values: [32]
  add_I3D_branch:
    values: [False]
  